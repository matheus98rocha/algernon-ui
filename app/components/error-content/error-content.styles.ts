import { Box } from "@mui/material";
import { styled } from "@mui/system";

export const WrapperErrorContent = styled(Box)(() => ({
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  height: "100vh",
  flexDirection: "column",
  gap: "1rem",
}));
